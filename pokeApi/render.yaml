# render.yaml

# No es necesario un comando de construcción para proyectos estáticos
buildCommand: ""

# No hay tareas pre-despliegue necesarias para proyectos estáticos
preDeployCommand: ""

# Comando de inicio para servir archivos estáticos con Python
startCommand: "python -m http.server $PORT"
